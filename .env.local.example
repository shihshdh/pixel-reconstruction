# Beam CPU 网关地址（部署 backend/beam 后填入，结尾不要带斜杠）。
# 这是公开服务地址，不是 API Key。不要把 Beam 密钥写到 NEXT_PUBLIC_*。
NEXT_PUBLIC_BEAM_API=
# 豆包默认密钥放在 Beam Secrets 的 ARK_API_KEY，不能放进 NEXT_PUBLIC_*。
# 用户自己的豆包 Key 可在网页“修图”设置里临时填写。
